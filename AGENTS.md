# Project identity and shared context

This project's current canonical name is **Arvena**. Its previous working name was **DetoxWay**; some earlier material also uses Santiago wellness terminology.

The project is undergoing rebranding and reconstruction, so legacy names may remain in code, documentation, infrastructure, URLs, content, and repository history. Do not rename technical identifiers or change infrastructure solely for branding consistency unless the user explicitly requests it.

Canonical project context is available in `../Projects/Arvena`. It contains the broader vision, purpose, product direction, and development stages. Codex may also consult documentation for related projects under `../Projects` when ecosystem context would materially improve the work.

Current user instructions and the actual repository state remain authoritative for the immediate task. Vision documents provide context; they do not authorize unrelated implementation.
