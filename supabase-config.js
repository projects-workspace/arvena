'use strict';

// Public configuration only. Never place a secret or service-role key here.
// Supply the dedicated Arvena project URL and publishable key before release.
window.ARVENA_SUPABASE_CONFIG = Object.freeze({
    url: '',
    publishableKey: '',
    table: 'arvena_mvp_requests'
});
